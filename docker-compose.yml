version: '3.9'

services:
    backend:
        build:
            dockerfile: Dockerfile
            context: ./backend
        container_name: django
        command: python manage.py runserver 0.0.0.0:8000
        ports:
            - '8000:8000'
        volumes:
            - ./backend/:/usr/src/app
